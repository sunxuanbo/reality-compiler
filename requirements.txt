# ============================================================
# Reality Compiler - 依赖清单
# 安装方式： pip install -r requirements.txt
# ============================================================

# Streamlit：唯一的运行时依赖，负责整个 Web 界面（输入框 / 按钮 / st.code 代码块）
streamlit>=1.38,<2.0

# requests：调用 DeepSeek API 发送 HTTP 请求（严禁使用 openai 库）
requests>=2.28,<3.0

# python-dotenv（可选）：读取项目根目录下的 .env 文件里的环境变量；
# 不安装也能跑（直接通过系统环境变量设置 DEEPSEEK_API_KEY 即可）。
python-dotenv>=1.0,<2.0

# reportlab：导出深色终端风格的 PDF 日志报告（v2.3）
reportlab>=4.0,<6.0

# pydantic：Schema 安全门（v2.6），严格校验 AI 返回数据，防止格式异常导致页面崩溃
pydantic>=2.0.0,<3.0
